class AddPhotoToCurrentPosts < ActiveRecord::Migration
  def up
    # Kid.all.each do |kid|
    #   kid.posts.each do |post|
    #     next if post.photo?

    #     post.update(photo: kid.photo)
    #   end
    # end
  end

  def down
  end
end

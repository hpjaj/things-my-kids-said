<%= simple_form_for :comment, url: url, method: http_verb do |f| %>
  <%= f.input :body, label: false, placeholder: 'Comment', required: false, as: :text %>
  <%= f.submit button_name, class: "pull-right btn devise-buttons #{button_class}" %>
<% end %>
